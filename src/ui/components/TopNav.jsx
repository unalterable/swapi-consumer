import React from 'react';
import AppBar from '@material-ui/core/AppBar';
import Toolbar from '@material-ui/core/Toolbar';

const ButtonAppBar = () => (
  <div>
    <AppBar position="static">
      <Toolbar>
        Star Wars Person Searcher
      </Toolbar>
    </AppBar>
  </div>
);

export default ButtonAppBar;
